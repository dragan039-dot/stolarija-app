-- CreateTable
CREATE TABLE "Params" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "grupa" TEXT NOT NULL,
    "naziv" TEXT NOT NULL
);

-- CreateTable
CREATE TABLE "Profil" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "naziv" TEXT NOT NULL
);
